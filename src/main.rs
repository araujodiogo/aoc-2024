mod days;

use crate::days::one::first_part::first_part;
use crate::days::one::second_part::second_part;

fn main() {
    first_part().expect("TODO: panic message");
    second_part().expect("TODO: panic message");
}
